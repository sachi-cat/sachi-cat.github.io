$(document).ready(function() {
    $('.carousel').slick({
        autoplay: true, // 自動再生
        autoplaySpeed: 5000, // 自動再生の速さ、単位はミリ秒
        // appendDots: ,
        // centerMode: true, //コメントアウトする
        dots: true,
        // fade: teue,
        // mobileFirst: ,
        arrows: false, // 左右のナビゲーションを無効にする・追加
    });
});
